# Searching-Algorithms

#Linear Search O(n)
Basic search from beginning to end checking all values until the key is found.

#Binary Search O(log(n))
Divide and conquer algorithm to reduce search time from linear to logarithmic time complexity.
